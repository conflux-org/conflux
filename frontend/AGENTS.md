# Frontend Guidelines

- **API URL Pattern & Trailing Slash**: All API request paths **MUST** follow the pattern `/api/<features>/.../` and end with a trailing slash `/`. Django backend routing requires trailing slashes to match endpoints correctly.
